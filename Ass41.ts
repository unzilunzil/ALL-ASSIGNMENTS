// Function to show magicians from the given array
function show_magicians0 (magicians : any) {
    for (let i = 0; i < magicians.length; i++) {
        console.log(magicians[i]);
    }
}

// Array of magician's names
const magicians = ['Merlin', 'David Copperfield', 'Houdini', 'Dynamo'];

// Calling the function to display magician's names
show_magicians0(magicians);
